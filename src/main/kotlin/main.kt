import java.util.*

fun main() {
    val scan = Scanner(System.`in`)
    while (true) {
        val line = scan.nextLine()
        if (line == "q") {
            break;
        }
        println(line)
    }
}
